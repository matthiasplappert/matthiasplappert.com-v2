// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  output: 'export',
  basePath: '/matthiasplappert.com-v2',
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
